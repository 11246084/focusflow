# FocusFlow 前端本機開發設定。
# 複製為同資料夾的 .env 後，重新啟動 Vite 才會套用變更。

# Backend REST API 基底網址；必填。本機 Backend 預設使用 4000 port。
VITE_API_BASE_URL=http://127.0.0.1:4000/api/v1

# LINE Bot 加入好友網址；選填，建議填入 https://line.me/R/ti/p/@你的官方帳號ID。
# 可從 LINE 官方帳號管理後台取得；留空時前端不會顯示一般加入好友網址。
VITE_LINE_BOT_URL=
