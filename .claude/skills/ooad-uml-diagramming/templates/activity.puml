@startuml
' ============================================
' ooad-phase:   需求分析
' chapter:      5-3
' realizes:     UC-xx
' source:       <使用個案描述>（文件）
' verified:     YYYY-MM-DD
' status:       draft
' implemented:  yes
' ai-assisted:  yes / <工具> / <使用範圍>
' ============================================
'
' [NTUB:MUST] 5-3 必須以活動圖描述使用個案。
'   使用個案描述表是補充，不是替代。
'   「線性流程不值得畫活動圖」的 heuristic 在 5-3 不適用。
'
' [UML:MUST] 必須有 start / stop
' 分支用 if/else；並行用 fork；多角色用泳道
' 本圖屬需求層 → 不出現內部物件、方法或端點
' ============================================
!include ../_style.puml

title <圖名，須與檔名中的名稱一致>

|使用者|
start
:選擇課程;
:輸入問題;

|系統|
:檢查提問權限;

if (有權限？) then (是)
  :檢索相關片段;

  if (找到足夠片段？) then (是)
    :產生回答與來源時間戳;
    |使用者|
    :檢視回答與來源;
  else (否)
    |系統|
    :回覆資料不足;
    |使用者|
    :檢視提示訊息;
  endif

else (否)
  |系統|
  :回覆無權限;
  |使用者|
  :檢視錯誤訊息;
endif

stop

@enduml
